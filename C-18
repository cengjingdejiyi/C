//采用递归方法，计算 x^n。其中，n 是正整数。注意：设计具有尽可能少的递归层次的算法。
#include<stdio.h>
double hanshu(double x,int n){
	if(n==0){
		return 1;
	}
	else if(n>0){
		return x*hanshu(x,n-1);//通过递归的思想将x的n次方转
		                       //变成x乘以x的n-1次方，
		                       //类推下去就变成n个x相乘，即可
							   //得到x的n次方 。 
	}
} 
int main(){
    int b;
    double a;
	double res; 
    scanf("%f,%d",&a,&b);
    res = hanshu(a,b);
    printf("%f",res);    
	return 0;
}
//结果 2，10
//     1024
