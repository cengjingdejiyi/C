//计算输出 s(n) = 1 −1/2 +1/3 −1/4 +1/5 − · · · + (−1)^n−1/1/n 的极限值。要求计算误差不大于10^−6。
#include<stdio.h>
#include<math.h>
int main(){
    int n=0;
    double S=0,a=0;
    double b=0; 
    scanf("%d",&n);
    for(a=1;a<=n;a++){
    	b =pow(-1,a-1)/a;//（（-1）^(a-1)) 计算方法有问题
		                 //应该换成pow(-1,a-1) 并加上头文件 
    	S += b;//S+=b等价于 S=S+b  
    }
	
    printf("%f",S);
    
	return 0;
}//结果：输入9，999，999时6位小数极限0.693147
