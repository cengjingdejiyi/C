//编程计算阶乘 n!。注意观察当 n 值稍大时，计算结果是否正确，并分析原因。
#include<stdio.h>
int main(){
	int num,a,res;
	scanf("%d",&num);
	if(num<0){
		printf("负数无意义");
	} 
	else{
	for(a=num;a>0;a--){//for函数内的内容用;分开,第一个为初始量,第二个为判定条件,
	                   //满足判定条件时进行第三个循环,不满足时进行输出 
	res*= a;
}
	printf("阶乘数为%d",res);//最大能算到19的阶乘，20数据溢出就为负数了 
	} 
	return 0;
} 
//结果：3
//      阶乘数为6  
